# Digal Architect & Builders

Responsive portfolio website for [digalarchitect.dev](https://digalarchitect.dev).

## Local preview

```bash
npm run dev
```

The production-ready static website is located in `dist/` and configured for Vercel through `vercel.json`.
